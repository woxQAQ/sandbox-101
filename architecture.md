# sandbox monorepo 

An overall design for sandbox monorepo.

# 0. intro

# 1. engine

engine 是运行在宿主机上的 microVM 引擎，负责启动 VM，管理 host 上 VM 的生命周期。
VM是由一组资源组成的，因此创建VM不能简单等同于创建进程（实际上进程也有生命周期），而是类似于 pod 一样有明确的状态转换
engine 驱动所有的 vm，事实上是整个系统的大脑，与vm相关的，存储，内存，快照，镜像，网络，设备等等都有 engine 统一管理
我们不会自己实现 VMM，毕竟基于 kvm 的 QEMU，Firecracker（fc），cloud hypervisor(CH) 等技术已经发展多年，相当成熟了。我们将直接基于 fc 作为 VMM 层来启动虚拟机
看完本设计，可以说是本系统是在发明沙盒系统的的 docker engine也不为过

> VM，microVM，VMM，kvm，docker。。。本文建立在读者熟悉这些概念的基础上。不懂也没关系，花几分钟时间问问大模型就行了

## why microVM

microVM 是权衡下的决定。如果使用容器，基于现有的容器运行时很容易就造出一套沙盒系统来，前文也说过本 engine 可能是在重新发明 docker engine。但容器本质仍然是进程，它共享 host kernel，无论容器安全发展得如何，这个隔离模型就决定了边界所在。

而模拟了完整设备的 VM 确实获得了独立的 guest kernel，但其引入了对我们来说没有必要的额外设备。由于这些 VM 系统构建了完整的设备模型，那么 microVM 就从这里出发，将设备模型进行裁剪。这样得到的 microVM 的启动速度就会变得很快了，当然还可以再进行极端的优化，比如引入 uffd + fuse 来做延迟载入。

> 但所有的一切都是有关于权衡。容器在这个场景会不会更好？真的需要极端冷启动优化吗？我们都用一句「看业务场景」轻轻带过，但真有业务场景了要怎么权衡？

## 存储层

从VMM的第一性原理来说，我们要存什么？

1. 这台机器上开了哪些vm？vm现在是什么状态？ 
2. vm工作在什么之上？更本质的说法是，vm所必需的磁盘设备，从哪来？怎么存？
3. vm会被中断，如何保存中断点并实现1:1恢复工作状态？

每一点都能延伸出很多话题

### 元数据

对于第一点，很显然的解决方案是我们为每台宿主机单独保存状态，数据直接存在机器上。理想的方案是 json 存储或者使用嵌入式数据库如 sqlite 等。

> 从理论上讲我们只需要kv数据库就好了，我们要存的东西通常是一个领域化的东西，比如 VM 的 id，镜像引用之类的，用 scoped key value pair 是最佳选择。

然而元数据层并不会因为引入了带原子性和事务的数据库，就一切都万事大吉了。我们系统的资源管理是不遵守数据库事务协议的，数据库事务能保证的只有数据库字段的原子性，然而对 VM 的所有状态的一致性保证是超过数据库事务能力的边界的。

meta层要做的其实是辅助vm生命周期管理。不过这部分要牵扯到 vm 的生命周期设计，因此暂且不展开。

### 镜像

第二点的门道比较多，涉及到整个 engine 架构的问题。首先想想我们的系统怎么启动的？BIOS，引导块，启动内核，init进程。我们需要加载磁盘中的内核到内存中才能启动内核，那么，就需要有个地方放我们的内核的二进制文件。回忆一下装系统的过程，我们会给磁盘分区，安装文件系统，然后把内核放到文件系统里。因此一个对我们来说最朴素的方案就是，每一个vm都占有这样一个文件系统——或者说一个文件目录，这个目录下存放着vm启动所必需的所有东西。

一个问题是怎么分发和共享？如果我们的系统只是跑固定的某几种系统，如 ubuntu，windows 的特定版本之类的，那么准备几个系统盘挂载即可，但这里的问题是，「如果我想让调用方自己决定沙盒的底座长什么样，比如我要一个完整的Ubuntu桌面，那总不能我开发沙盒的人天天去给调用方做系统盘吧？」

这里就要想到我们伟大的 docker 的发明——容器镜像。docker的核心在于分发一套固定的环境，使得应用程序在不同的宿主上的表现都一样，这解决了软件开发经典的「我的机器上跑得好好的」的问题。容器镜像后来演化出了 OCI 镜像规范。有一个常见的说法是 「镜像的本质是Linux 根目录的tarball」，当然实际去拉一个镜像并解包我们会得到一堆的tarball，每一个tarball对应一个层，层与层叠加最后会得到完整的最终目录。这种层次结构的好处在于，docker pull的对象不再是完整的镜像，而是一个层——我们对每个层都计算内容哈希，这样当镜像更新后就只需要拉取哈希值发生变化的层。

我们要的不就是这样的东西吗！我们可以利用 Dockerfile 让用户去自己写镜像，只要这个镜像符合我们的要求（它必须是一个合法的系统镜像）。

引入 OCI 镜像的第二个理由是，我们可以在镜像提供的 unionfs 基础上再叠加层次，让每个 vm 都有自己的可写层。这里的工程意义是，我们只需要为每个vm单独分配可写的磁盘设备就行了。底层的系统盘是宿主机级别共享的，只需要在元数据层保留一个引用就可以了，而且我们的元单位是层，所以我们的引用可以精确到层级别的引用，从缓存层面看效率更高了。鉴于我们还需要实现快照功能，这样的设计能使得我们在快照的时候只关注vm的可写磁盘，不需要拷贝底层 imutable 的系统镜像。

通常我们采用 overlayfs 去做叠加，COW是 overlayfs 的一个突出特性，这使得我们能去共享底层不可变的引用，仅在修改时才去做复制覆盖。不过这也会带来复制放大的问题，——overlayfs 的 cow 是文件级别的，修改一个字节或者修改权限会导致整个文件的复制
> infra 设计要考虑权衡问题。由于内核文件通常不可变，可变的部分又比较小，我们也不会经常改这些文件，这里的复制放大成本不高，反而可以利用overlayfs的优势

### 快照

一个运行中的 VM 包含的状态包括磁盘（在上面一节已经详细说过了）、内存、CPU、设备（网络也是设备的一种）等。最初我们将 engine 比作 docker，

底层 VMM（firecracker）已经为我们封装了 VM 快照的能力，但它没有办法快照我们在前一节提出的可写磁盘的内容。由于我们需要dump整个vm的快照，因此必须 stw(stop the world)。整体时序如下

1. 获取vm的操作锁，这是新引入的概念，所有涉及 VM 生命周期的操作都要获得互斥的操作锁
2. 暂停 VM 
3. 调 VMM snapshot API
4. 复制 engine 创建出来的可写层
5. 恢复 VM

> 这里要用到分布式系统经常用到的原子发布，创建出来的快照先写到一个中间文件，然后再提交到正式的store里

snapshot支持两种操作：restore和clone。restore只允许原vm使用来回到原来的状态。clone则是根据 snapshot 的内容派生一个 vm。restore会造成一种危险的情况，就是混代快照的问题：由于我们的快照显然是有多个文件组成的，那么我们的快照是逐个恢复的，这就会存在中间态——有一部分文件已经恢复到快照 B 了但还有一部分文件还在快照 A 的状态。由于各种原因，最终我们得到的目录是一个混代的目录，此时如果简单的启动vm不仅仅会导致失败，而且可能造成不可逆的破坏。更有可能的是，vm甚至不会失败，而是静默运行，直到产生资产损失。

这里的处理方式是如果restore过程出现部分失败，那么就需要隔离这个 vm，不允许其被启动，只允许再次 restore 或删除 vm。

## 网络层

网络层的设计只关注一件事：VM怎么和外部网络打通？

### virtio

前面介绍存储层的时候并没有介绍 firecracker 虚拟化设备所用的 virtio。virtio 是一种半虚拟化技术，何为半虚拟化技术？

1. 完全软件虚拟化，比如 QEMU，他用软件模拟真实硬件，但缺点很明显，性能差而且设备模型很大
2. 硬件直通：直接把真实设备交给VM，优势是性能极佳，但VM和真实设备依赖，导致兼容性不好，也不适合快照，克隆场景

软件虚拟化的问题是什么？Guest要求真实设备，使得VMM为了对齐真实设备的行为而维护大量的虚拟设备代码。那么解决方案是什么？virtIO的方案是直接给虚拟设备，内核明确知道这是一个虚拟设备，直接走专门的代码处理。

FC VM 在网络层使用的是 virtio-net 网卡，不过他解决的是 guest VM 和 VMM(FC) 的网络

### TAP
